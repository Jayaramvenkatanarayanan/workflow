name: 04 - Using Actions
on:
    push
jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - name: checkout-code
          uses: actions/checkout@v4
          with:
            fetch-depth: 0
        - name: print the directory
          run: ls