name: building blocks
on: push

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: echo "hello world"
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: say goodbye
        run: |
          echo " this fail message"
          exit 1
      - name: say goodbye
        run: |
          echo "Hello it's good bye message"
