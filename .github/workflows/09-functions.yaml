name: 09 - Using Functions
run-name: 09 - Using Functions
on:
  pull_request:
  workflow_dispatch:
jobs:
  echo1:
    runs-on: ubuntu-latest
    steps:
      - name: Failing step
        run: |
          echo "should exit with a non-zero code"
          exit 1
